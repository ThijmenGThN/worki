name: Deploy

on:
  push:
    branches: [ server ]

jobs:
  Deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout Git
        uses: actions/checkout@v3

      - name: Install
        run: npm i
        
      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Start
        run: npm start
