name: server

on:
  push:
    branches: [ server ]

jobs:
  Fetch:
    runs-on: self-hosted
    steps:
      - name: Checkout Git
        uses: actions/checkout@v3

  Validate:
    runs-on: self-hosted
    needs: [Fetch]
    steps:
      - name: Install
        shell: npm install
      - name: Lint
        shell: npm run lint

  Build:
    runs-on: self-hosted
    needs: [Validate]
    steps: 
      - name: Build (NextJS)
        shell: npm run build
      - name: Build (Docker)
        shell: docker-compose build

  Deploy:
    runs-on: self-hosted
    needs: [Build]
    steps:
      - name: Deploy
        shell: docker-compose up -d
